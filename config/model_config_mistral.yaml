# =============================================================================
# Ministral 8B — only model-specific fields
# =============================================================================
model:
  base_model_id: "unsloth/Ministral-8B-Instruct-2410-bnb-4bit"
  hub_model_id: "staerkjo/checkpoints-mistral"
  max_seq_length: 2048
  load_in_4bit: true
  eos_token: "</s>"

wandb:
  run_name: "ministral-8b"
  tags:
    - "qlora"
    - "mistral"
